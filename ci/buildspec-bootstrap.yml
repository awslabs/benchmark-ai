version: 0.2

phases:
  install:
    runtime-versions:
      docker: 18
  build:
    commands:
      - env
      - $(aws ecr get-login --region $AWS_DEFAULT_REGION --no-include-email)
      - make bootstrap_publish BOOTSTRAP_DOCKER_URL=${BOOTSTRAP_DOCKER_URL}
