version: 0.2

#env:
  #variables:
     # key: "value"
     # key: "value"
  #parameter-store:
     # key: "value"
     # key: "value"

phases:
  #install:
    #runtime-versions:
      # name: version
      # name: version
    #commands:
      # - command
      # - command
  #pre_build:
    #commands:
      # - command
      # - command
  build:
    commands:
      - cd ${project}
      - make install deploy.yml COMMIT_SHORT_HASH=$CODEBUILD_RESOLVED_SOURCE_VERSION
  #post_build:
    #commands:
      # - command
      # - command
artifacts:
  files:
    - deploy.yml
  #name: $(date +%Y-%m-%d)
  discard-paths: yes
  base-directory: ${project}
#cache:
  #paths:
    # - paths