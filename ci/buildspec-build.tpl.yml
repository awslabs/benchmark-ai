version: 0.2

env:
  variables:
    COMMIT_SHORT_HASH: $CODEBUILD_RESOLVED_SOURCE_VERSION
    VERSION: "latest"
  #parameter-store:
     # key: "value"
     # key: "value"

phases:
  #install:
    #runtime-versions:
      # name: version
      # name: version
    #commands:
      # - command
      # - command
  #pre_build:
    #commands:
      # - command
      # - command
  build:
    commands:
      - cd ${project}
      - make
  post_build:
    commands:
      - make deploy.yml
artifacts:
  files:
    - deploy.yml
    - deploy/**/*
  #name: $(date +%Y-%m-%d)
  discard-paths: no
  base-directory: ${project}
#cache:
  #paths:
    # - paths