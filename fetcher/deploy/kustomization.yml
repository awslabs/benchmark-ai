bases:
  - local
images:
  - name: benchmarkai/fetcher
    newName: benchmarkai/fetcher:765bc33
configMapGenerator:
  - name: fetcher-dispatcher
    literals:
      - job_image=benchmarkai/mock-fetcher-job:local-latest
    behavior: merge
