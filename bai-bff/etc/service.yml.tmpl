apiVersion: v1
kind: Service
metadata:
  name: @@PROJECT@@
  annotations:
    service.beta.kubernetes.io/load-balancer-source-ranges: @@PREFIX_LIST_CIDR_BLOCKS@@
spec:
  selector:
    app: benchmark-ai
    service: @@PROJECT@@
    env: @@ENVIRONMENT@@
  ports:
  - protocol: TCP
    name: http
    port: 80
    targetPort: @@SVC_PORT@@
  - protocol: TCP
    name: jmx
    port: @@JMX_PORT@@
    targetPort: @@JMX_PORT@@
  type: LoadBalancer
