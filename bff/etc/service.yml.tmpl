apiVersion: v1
kind: Service
metadata:
  name: @@PROJECT@@
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-extra-security-groups: "sg-069f9642db2fa41fe"
spec:
  selector:
    app: benchmark-ai
    service: @@PROJECT@@
    env: @@ENVIRONMENT@@
  ports:
  - protocol: TCP
    port: 80
    targetPort: @@SVC_PORT@@
  type: LoadBalancer
